{
    "configurations": [
        {
            "name": "Launch Program",
            "program": "${workspaceFolder}/${file}",
            "request": "launch",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "pwa-node"
        }    
    ]
}